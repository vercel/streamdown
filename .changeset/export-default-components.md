---
"streamdown": patch
---

Export `defaultComponents` so custom `components` overrides can compose Streamdown's built-in renderers (styles, `data-streamdown`, link safety, code/mermaid/table behavior) instead of reimplementing them.