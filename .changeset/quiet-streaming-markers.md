---
"remend": patch
"streamdown": patch
---

Preserve whitespace after empty list markers and add opt-in `pendingInlineMarkers` handling to hold ambiguous emphasis openers during streaming. Streamdown reveals remaining literal markers when `isAnimating` becomes false.
