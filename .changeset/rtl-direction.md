---
"streamdown": minor
---

Add a `dir` prop that accepts `"ltr"`, `"rtl"`, or `"auto"`. When set to `"auto"`, each block's text direction is detected by scanning for the first strong Unicode character (Arabic, Hebrew, Thaana, etc.).
