---
"streamdown": patch
---

Add a granular `controls` prop that accepts a boolean to toggle all controls or an object with per-feature flags (`code`, `table`, `mermaid`) for fine-grained control over copy, download, fullscreen, and pan/zoom buttons.
